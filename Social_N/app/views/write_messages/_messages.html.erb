<%= simple_form_for Conversation.new do |conversation| %>
  <%= conversation.hidden_field :sender_id, value: current_user.id %>
  <%= conversation.hidden_field :recipient_id, value: user.id %>

  <%= conversation.submit "Write message" %>
<% end %>